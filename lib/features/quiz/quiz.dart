/// Higher-or-lower quiz over the carbon datasets (decision E8).
///
/// Cross-domain by design: rounds rotate between home energy, food and
/// transport, and a pair is always drawn from one of them.
library;

export 'presentation/providers/quiz_providers.dart';
export 'presentation/screens/higher_or_lower_screen.dart';
